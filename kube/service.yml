apiVersion: v1
kind: Service
metadata:
  name: hello-service
spec:
  selector:
    app: rails
  type: LoadBalancer
  ports:
    - port: 80
      protocol: HTTP
      targetPort: 3000
  # loadBalancerSourceRanges:
  #   - ""
